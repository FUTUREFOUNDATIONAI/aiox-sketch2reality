# Story 2.1: REIMAGINE Variant Generation

## Status

Draft

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [code-review, architecture-review]

## Story

**As a** presenter,
**I want** to generate 3 variant interpretations of the design after the initial generation,
**so that** the audience sees multiple creative possibilities from one sketch.

## Acceptance Criteria

1. GENERATE VARIANTS button appears after successful initial generation
2. Calls Stitch `generate_variants` with `creativeRange: REIMAGINE` and 3 variants
3. Variants are displayed in a horizontal gallery row below the main result
4. Each variant shows a screenshot/preview and can be selected for full preview
5. Selected variant can be deployed to its own Vercel URL
6. Variant generation uses same branding constraints as initial generation

## Dependencies

- Story 1.5 (End-to-End Integration & Live Preview) — needs the complete pipeline working first
- Story 1.4 (Automatic Vercel Deployment) — variant deployment reuses this

## Tasks / Subtasks

- [ ] Add GENERATE VARIANTS button (AC: 1)
  - [ ] Show button only after successful initial generation
  - [ ] Style consistently with existing UI branding
  - [ ] Disable during variant generation
- [ ] Implement Stitch `generate_variants` API call (AC: 2)
  - [ ] Create `POST /api/variants` endpoint on backend
  - [ ] Call Stitch `generate_variants` with `creativeRange: REIMAGINE`
  - [ ] Request 3 variants
  - [ ] Pass the original screen reference
- [ ] Build horizontal variant gallery (AC: 3)
  - [ ] Create gallery row layout below main result
  - [ ] Display 3 variant cards side by side
  - [ ] Apply consistent branding to gallery
- [ ] Implement variant selection and preview (AC: 4)
  - [ ] Each variant card shows screenshot/preview thumbnail
  - [ ] Click on variant loads it in the main preview iframe
  - [ ] Visual indicator for selected variant
- [ ] Enable variant deployment (AC: 5)
  - [ ] Add deploy button per variant or for selected variant
  - [ ] Reuse Vercel deployment logic from Story 1.4
  - [ ] Generate unique URL per variant deployment
  - [ ] Display deployed URL for variant
- [ ] Maintain branding constraints (AC: 6)
  - [ ] Pass same Future Foundation branding prompt to variant generation
  - [ ] Verify variants maintain brand identity

## Dev Notes

**Epic:** Epic 2 — Stage Experience: Polish & Variants

**Technical Context:**
- Stitch MCP provides `generate_variants` tool for creating design variations
- `creativeRange: REIMAGINE` creates completely different layouts while maintaining brand identity
- FR8: System SHALL provide a "Generate Variants" feature that creates 3 REIMAGINE variants
- Variant generation reuses the same branding constraints defined in Story 1.1
- Each variant can independently go through the HTML extraction and Vercel deployment pipeline
- Stitch Project ID: `6851128296893269757`

**Stitch `generate_variants` Parameters:**
- Screen reference from initial generation
- `creativeRange: REIMAGINE` for maximum creative variation
- Count: 3 variants

### Testing

- Manual testing of variant generation flow
- Verify 3 variants are generated and displayed
- Test variant selection and preview
- Test variant deployment to unique URLs
- No unit tests required for MVP

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-20 | 1.0 | Story created from PRD | @sm (River) |

## Dev Agent Record

### Agent Model Used

_To be filled during implementation_

### Debug Log References

_To be filled during implementation_

### Completion Notes List

_To be filled during implementation_

### File List

_To be filled during implementation_
