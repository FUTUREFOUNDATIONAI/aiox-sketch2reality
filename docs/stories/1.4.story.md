# Story 1.4: Automatic Vercel Deployment

## Status

Draft

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [code-review, architecture-review]

## Story

**As a** presenter,
**I want** generated websites to automatically deploy to Vercel with a predictable URL,
**so that** I can share the link with the audience immediately.

## Acceptance Criteria

1. Vercel project is pre-configured with a known subdomain pattern
2. After HTML generation, system automatically creates a Vercel deployment
3. Deployment uses Vercel API (not CLI) for speed and reliability
4. Deployment completes within 30 seconds
5. Returned URL follows predictable pattern (e.g., `sketch-{timestamp}.vercel.app`)
6. `POST /api/generate` response includes the deployed URL
7. System supports pre-established custom domain if configured

## Dependencies

- Story 1.1 (Express Server with Stitch MCP Proxy) — needs the Express server
- Story 1.3 (HTML Code Extraction) — needs the extracted HTML to deploy

## Tasks / Subtasks

- [ ] Set up Vercel project configuration (AC: 1)
  - [ ] Pre-configure Vercel project with known subdomain pattern
  - [ ] Store Vercel API token in environment variables
  - [ ] Define URL pattern: `sketch-{timestamp}.vercel.app` or similar
- [ ] Implement Vercel API deployment (AC: 2, 3)
  - [ ] Create deployment module using Vercel REST API
  - [ ] Upload HTML file as single-file deployment
  - [ ] Handle Vercel API authentication
- [ ] Ensure deployment speed (AC: 4)
  - [ ] Optimize API calls for minimal latency
  - [ ] Implement timeout handling (30 second target)
- [ ] Generate predictable URLs (AC: 5)
  - [ ] Implement URL pattern with timestamp or unique ID
  - [ ] Verify URL is accessible after deployment
- [ ] Integrate with generate endpoint (AC: 6)
  - [ ] Add deployment step to `POST /api/generate` pipeline
  - [ ] Include deployed URL in response JSON
- [ ] Support custom domain (AC: 7)
  - [ ] Check for custom domain configuration in env vars
  - [ ] Apply custom domain alias if configured

## Dev Notes

**Epic:** Epic 1 — Core Pipeline: Sketch to Deployed Website

**Technical Context:**
- Deployment Target: Vercel (for generated websites)
- Use Vercel API (not CLI) for speed and reliability — critical for live demos
- NFR1: End-to-end generation time (upload to deployed URL) MUST be under 90 seconds total
- NFR4: The Vercel deployment link MUST be pre-established/predictable so the presenter can share it before the demo starts
- Vercel CLI or API is available for automated deployment (PRD says use API)
- Pre-established Vercel project with custom domain/subdomain for predictable URLs
- Vercel API token must be stored securely in environment variables

**Vercel API Reference:**
- Deployments API: `POST https://api.vercel.com/v13/deployments`
- Authentication: Bearer token via `Authorization` header
- Single-file deployment: upload HTML as the only file

### Testing

- Manual testing of deployment flow
- Verify URL is accessible after deployment
- Test deployment speed (target: under 30 seconds)
- No unit tests required for MVP

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-20 | 1.0 | Story created from PRD | @sm (River) |

## Dev Agent Record

### Agent Model Used

_To be filled during implementation_

### Debug Log References

_To be filled during implementation_

### Completion Notes List

_To be filled during implementation_

### File List

_To be filled during implementation_
